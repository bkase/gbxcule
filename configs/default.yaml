# Default configuration for GBxCuLE harness

benchmark:
  warmup_steps: 512
  steps: 8192
  sync_every: 64

verify:
  steps: 1024
  compare_every: 1

runtime:
  frames_per_step: 24
  release_after_frames: 8
